package com.seiuh.smartroomapp.ui.theme

import androidx.compose.ui.graphics.Color

// --- BACKGROUNDS ---
val RichBlack = Color(0xFF09090B)      // Nền chính App (Rất đen)
val Charcoal = Color(0xFF18181B)       // Nền phụ
val DarkGray = Color(0xFF27272A)       // Nền Card/Surface

// --- ACCENTS (Màu điểm nhấn) ---
val LimeGreen = Color(0xFFBFFE01)      // Màu thương hiệu chính (Nút bấm, Toggle ON)
val ElectricBlue = Color(0xFF38A0FF)   // Màu phụ (Icon lạnh, điều hòa)
val FireOrange = Color(0xFFFF5F1F)     // Màu phụ (Nhiệt độ nóng)

// --- TEXT & ICONS ---
val White = Color(0xFFFFFFFF)
val LightGray = Color(0xFFE4E4E7)      // Text phụ
val MutedGray = Color(0xFFA1A1AA)      // Text bị disable hoặc hint

// --- STATUS ---
val Danger = Color(0xFFEF4444)
val Success = Color(0xFF22C55E)